[package]
name = "porter"
version = "1.0.41"
edition = "2024"
repository = "https://github.com/umi-labs/porter"
description = "Porter is a high-performance, Rust-based CLI tool for migrating structured content between different systems. It supports complex nested data structures, parallel processing, and provides an intuitive interactive interface for data transformation."
license = "MIT"
authors = ["Isaac <isaac@umidigital.co.uk>"]
homepage = "https://github.com/umi-labs/porter"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
json5 = "0.4"
serde_yaml = "0.9"
clap = { version = "4", features = ["derive"] }
toml = "0.8"
thiserror = "1"
dialoguer = { version = "0.11", features = ["fuzzy-select"] }
colored = "2"
regex = "1"
log = "0.4"
env_logger = "0.11"
indexmap = "2"
rayon = "1.10"
glob = "0.3"
walkdir = "2.5"
libloading = "0.8"
swc_core = { version = "0.90", features = [
    "ecma_parser",
    "ecma_ast", 
    "ecma_visit",
    "common",
] }
swc_ecma_parser = "0.143"
swc_common = { version = "0.33", features = ["tty-emitter"] }
swc_ecma_visit = "0.98"
dashmap = "5.5"
chrono = { version = "0.4", features = ["serde"] }
num_cpus = "1.16"
futures = "0.3"
reqwest = { version = "0.11", features = ["json", "rustls-tls"], default-features = false }
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
tempfile = "3.8"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
