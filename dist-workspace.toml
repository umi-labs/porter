[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.29.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["homebrew"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# A GitHub repo to push Homebrew formulas to
tap = "umi-labs/homebrew-tap"
# Publish jobs to run in CI
publish-jobs = ["homebrew"]

# Each target gets its own section with a runner key
[dist.github-custom-runners.x86_64-unknown-linux-gnu]
runner = "blacksmith-4vcpu-ubuntu-2204"

[dist.github-custom-runners.aarch64-unknown-linux-gnu]
runner = "blacksmith-4vcpu-ubuntu-2204"

[dist.github-custom-runners.aarch64-apple-darwin]
runner = "blacksmith-4vcpu-macos-14"

[dist.github-custom-runners.x86_64-apple-darwin]
runner = "blacksmith-4vcpu-macos-13"

[dist.github-custom-runners.x86_64-pc-windows-msvc]
runner = "blacksmith-4vcpu-windows-2022"

# Global runner for non-target-specific tasks
[dist.github-custom-runners.global]
runner = "blacksmith-4vcpu-ubuntu-2204"
